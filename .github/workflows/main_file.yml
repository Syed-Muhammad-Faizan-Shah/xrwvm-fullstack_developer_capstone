name: Lint Code

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  placeholder:
    name: Placeholder Job (No Linting)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Workflow Active
        run: echo "GitHub Action is running, but linting is removed."
